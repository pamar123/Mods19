// Types for custom commands
declare global {
    namespace Cypress {
      interface Chainable {
        // add custom commands here if needed
      }
    }
  }
  
  // Add any custom commands here
  // Example: Cypress.Commands.add('customCommand', () => { ... })
  
  export {};